#
# Splunk app configuration file
#

[install]
is_configured = 0

[ui]
is_visible = 1
label = SecuritySavedSearches

[launcher]
author = Adam Hlavacek
description = An application with saved scheduled searches that were generated using Sigma rules
version = 0.0.38

[package]
check_for_updates = true
id = security_saved_searches

[triggers]
reload.addon_builder = simple